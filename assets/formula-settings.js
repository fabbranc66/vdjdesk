const formulaDefaults={energy:{spotify_energy:.55,loudness:.20,dance:.15,tempo:.10},singability:{popularity:.45,vocal:.35,valence:.20,speech_penalty:20},floor:{dance:.40,spotify_energy:.25,loudness:.15,tempo:.10,valence:.10},familiarity:{popularity:1},risk:{familiarity:.45,floor:.35,singability:.20,speech_penalty:10,instrumental_penalty:10},peak:{spotify_energy:.40,loudness:.25,dance:.20,popularity:.15},recovery:{familiarity:.30,floor:.25,singability:.20,valence:.15,inverse_risk:.10}};
const formulaTooltips={spotify_energy:'Energia audio Spotify: intensità e attività percepita, normalizzata da 0 a 100.',loudness:'Volume medio percepito della traccia, normalizzato da 0 a 100.',dance:'Ballabilità Spotify: regolarità ritmica e facilità di ballo, da 0 a 100.',tempo:'Incidenza del BPM adattata al genere musicale, da 0 a 100.',popularity:'Popolarità Spotify della traccia, da 0 a 100.',vocal:'Presenza vocale stimata come inverso della strumentalità, da 0 a 100.',valence:'Tono emotivo positivo e allegro della traccia, da 0 a 100.',speech_penalty:'Punti di penalità applicati quando il contenuto parlato supera la soglia.',familiarity:'Punteggio KR di familiarità già calcolato, da 0 a 100.',floor:'Potenza pista KR: capacità stimata di sostenere il ballo, da 0 a 100.',singability:'Cantabilità KR: probabilità che il pubblico conosca e canti il brano, da 0 a 100.',instrumental_penalty:'Punti aggiunti al rischio quando la strumentalità supera il 50%.',inverse_risk:'Peso applicato alla sicurezza del brano, calcolata come 100 meno rischio.'};
function formulaWeights(){try{return JSON.parse(state.bootstrap?.settings?.kr_formula_weights||'{}')}catch(error){return {}}}
function fillFormulaSettings(){const saved=formulaWeights();$$('[data-formula-group]').forEach(input=>{const group=input.dataset.formulaGroup,key=input.dataset.formulaKey;input.value=saved[group]?.[key]??formulaDefaults[group][key]});syncFormulaSetting()}
$$('[data-formula-key]').forEach(input=>{const label=input.closest('label');if(label){label.title=formulaTooltips[input.dataset.formulaKey]||'';label.classList.add('formula-help')}});
function syncFormulaSetting(){const value={};$$('[data-formula-group]').forEach(input=>{const group=input.dataset.formulaGroup,key=input.dataset.formulaKey;(value[group]??={})[key]=Number(input.value)});$('#kr-formula-weights').value=JSON.stringify(value)}
document.addEventListener('input',event=>{if(event.target.matches('[data-formula-group]'))syncFormulaSetting()});
$('#reset-formulas').addEventListener('click',()=>{$$('#formula-settings input').forEach(input=>{input.value=formulaDefaults[input.dataset.formulaGroup][input.dataset.formulaKey]});syncFormulaSetting();toast('Formule predefinite ripristinate · premi Salva impostazioni')});
$('#recalculate-formulas').addEventListener('click',async event=>{syncFormulaSetting();await post('settings',{kr_formula_weights:$('#kr-formula-weights').value});event.currentTarget.disabled=true;try{const result=await post('recalculate-kr',{});toast(`${Number(result.updated).toLocaleString('it-IT')} brani ricalcolati`);await refreshBootstrap()}catch(error){toast(error.message)}finally{event.currentTarget.disabled=false}});
setTimeout(fillFormulaSettings,500);
