function libraryParams(limit,offset){return new URLSearchParams({q:$('#library-search')?.value.trim()||'',bpm:$('#filter-bpm')?.value||'',musical_key:$('#filter-key')?.value||'',genre:$('#filter-genre')?.value||'',year:$('#filter-year')?.value||'',quality:$('#filter-quality')?.value||'',folder_root:$('#filter-folder')?.value||'',...(state.libraryExtraFilters||{}),limit:String(limit),offset:String(offset)})}
loadTracks=async function(silent=false,append=false){if(state.libraryLoading)return;state.libraryLoading=true;const button=$('#load-more-tracks');if(button)button.disabled=true;try{const offset=append?state.tracks.length:0;const params=libraryParams(60,offset);const data=await api(`tracks&${params}`);state.tracks=append?[...state.tracks,...data.items]:data.items;state.libraryTotal=data.total;renderTracks();fillTrackSelect();$('#results-count').textContent=`${state.tracks.length.toLocaleString('it-IT')} di ${data.total.toLocaleString('it-IT')}`;const query=$('#library-search')?.value.trim();const folder=$('#filter-folder');const activeIssue=Object.keys(state.libraryExtraFilters||{}).length?' - filtro Studio':'';$('#results-title').textContent=query?`Risultati per ${query}${activeIssue}`:folder?.value?folder.options[folder.selectedIndex].text:`Tutti i brani${activeIssue}`;button.classList.toggle('hidden',!data.has_more);if(!silent)showView('library')}finally{state.libraryLoading=false;if(button)button.disabled=false}};
currentLibraryParams=function(limit,offset){return libraryParams(limit,offset)};
$('#filter-quality').addEventListener('change',()=>{state.libraryExtraFilters={};loadTracks(false,false)});
$('#clear-filters').addEventListener('click',()=>{state.libraryExtraFilters={};$('#filter-quality').value=''},true);
